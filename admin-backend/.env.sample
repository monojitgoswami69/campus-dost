# =====================
# SC-CSE Chatbot Admin Backend - Environment Configuration
# =====================
# Copy this file to .env and fill in your actual credentials and API keys.
# 
# IMPORTANT: This file contains only sensitive values (API keys, credentials).
# All other configuration is in app/config.py with sensible defaults.

# =====================
# Authentication (REQUIRED)
# =====================

# JWT secret for token signing (minimum 32 characters, use strong random string)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-strong-random-secret-key-here-minimum-32-chars

# Admin credentials for login
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password

# =====================
# Firebase / Firestore Credentials (REQUIRED)
# =====================

# Base64-encoded service account JSON (for containerized deployments)
FIREBASE_CRED_BASE64=

# =====================
# Google Gemini AI API Keys (REQUIRED)
# =====================

# Comma-separated API keys for load balancing and rate limit management
GEMINI_API_KEYS_CSV=AIza_your_key_1,AIza_your_key_2

# =====================
# Model IDs (subject to change as models are updated)
# =====================

# Embedding model for vector generation 
GEMINI_EMBEDDING_MODEL=gemini-embedding-001

# OCR model for PDF/image text extraction
GEMINI_OCR_MODEL=gemini-2.5-flash

# =====================
# GitHub Integration (for system instructions sync)
# =====================

# GitHub personal access token (needs repo read/write permissions)
GITHUB_TOKEN=ghp_your_token_here

# Repository where system instructions are stored
GITHUB_REPO=your_username/your_repo

# Branch to sync from/to
GITHUB_BRANCH=main

# Path to system instructions file in the repository
GITHUB_SYS_INS_PATH=path/to/system_instruction.txt

# =====================
# Dropbox Storage 
# =====================

# Dropbox app credentials 
DROPBOX_APP_KEY=your_app_key_here
DROPBOX_APP_SECRET=your_app_secret_here
DROPBOX_REFRESH_TOKEN=your_refresh_token_here